# 02 — Estrutura de camadas

**Status:** ✅ Concluído

Criar as pastas `src/domain/{types,rules}`, `src/data`, `src/ui/{pages,components,hooks}` conforme a seção 2 de `docs/arquitetura.md`, com a regra de dependência de mão única (`ui` → `data`/`domain`, nunca o contrário).

Critério de pronto: estrutura de pastas criada e vazia, pronta para receber os arquivos das tarefas seguintes.
